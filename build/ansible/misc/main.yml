---
- name: miscellaneous tasks
  hosts: all
  tasks:

    - name: Update all packages to their latest version
      ansible.builtin.apt:
        name: "*"
        update_cache: yes
      retries: 5
      delay: 30
      register: result
      until: result.rc == 0